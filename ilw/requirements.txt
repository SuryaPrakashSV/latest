SELECT
    row_count,
    COUNT(*) AS how_many_tasks_have_this_many_rows
FROM (
    SELECT
        COALESCE(
            NULLIF("great_grandbaby_task_id", ''),
            NULLIF("grandbaby_task_id", ''),
            NULLIF("baby_task_id", ''),
            NULLIF("grandchild_task_id", ''),
            NULLIF("child_task_id", ''),
            NULLIF("parent_task_id", '')
        ) AS task_id,
        COUNT(*) AS row_count
    FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP."Wrike_STG"
    GROUP BY 1
    HAVING COALESCE(
            NULLIF("great_grandbaby_task_id", ''),
            NULLIF("grandbaby_task_id", ''),
            NULLIF("baby_task_id", ''),
            NULLIF("grandchild_task_id", ''),
            NULLIF("child_task_id", ''),
            NULLIF("parent_task_id", '')
        ) IS NOT NULL
)
GROUP BY row_count
ORDER BY row_count DESC;
