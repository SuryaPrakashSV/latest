SELECT DISTINCT
    'WRIKE_API_BAU_PROJECT_LEVEL' AS source_table,
    "PROJECT_ID" AS stored_id,
    "PROJECT_NAME" AS stored_name
FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP.WRIKE_API_BAU_PROJECT_LEVEL
WHERE "PROJECT_ID" = 'IEADQP2VI4RS6ZFW'
   OR "PROJECT_NAME" ILIKE '%BTS EMEA Takedown%'

UNION ALL

SELECT DISTINCT
    'WRIKE_PROJECT_STATUS_HISTORY_SCD' AS source_table,
    "PROJECT_ID" AS stored_id,
    "PROJECT_NAME" AS stored_name
FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP.WRIKE_PROJECT_STATUS_HISTORY_SCD
WHERE "PROJECT_ID" = 'IEADQP2VI4RS6ZFW'
   OR "PROJECT_NAME" ILIKE '%BTS EMEA Takedown%'

UNION ALL

SELECT DISTINCT
    'WRIKE_TEST' AS source_table,
    "ID" AS stored_id,
    "TITLE" AS stored_name
FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP.WRIKE_TEST
WHERE "ID" = 'IEADQP2VI4RS6ZFW'
   OR "TITLE" ILIKE '%BTS EMEA Takedown%'

ORDER BY source_table, stored_id;
