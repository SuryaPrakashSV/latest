Hi Akash, I checked six completed tasks across parent, child, and grandchild levels.

In the Snowflake results, all six showed only Completed; I found no earlier status records. I then tested the same six IDs through the Wrike API. The current-task request returned all six as Completed. The task-history request succeeded with HTTP 200, but returned only task IDs, with no status changes or transition timestamps.

I also tried opening a sampled task’s permalink to check its UI history, but my browser account shows “You don’t have access.”

So far, I haven’t retrieved the earlier transient states from the sources tested. Could you help arrange read access to one sampled task, or share its status-change history screenshot? That would let me compare a known earlier transition against the API response. I’ll share the evidence screenshots and keep incremental work paused.
