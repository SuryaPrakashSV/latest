SELECT DISTINCT
    "id" AS project_id,
    "parent_task_id",
    "child_task_id",
    "grandchild_task_id",
    "baby_task_id",
    "grandbaby_task_id",
    "great_grandbaby_task_id",
    "status",
    "customStatusId",
    "updatedDate",
    "completedDate"
FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP."Wrike_STG"
WHERE UPPER(TRIM(TO_VARCHAR("status"))) = 'COMPLETED'
LIMIT 20;
